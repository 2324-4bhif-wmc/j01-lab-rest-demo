package at.htl;

import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
public class BalaszResourceIT extends BalaszResourceTest {
    // Execute the same tests but in packaged mode.
}
